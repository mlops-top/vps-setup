---
- name: Install system packages
  ansible.builtin.apt:
    package:
      - curl
      - tmux
    state: present

- name: Update system packages to the latest version
  ansible.builtin.apt:
    upgrade: safe
  tags:
    - molecule-idempotence-notest # Upgrading to the latest version is not idempotent.
