[vps]
# inventory.initial.ini:   first run over the public ip as root
# inventory.provision.ini: later run over the tailscale ip as ansible
host ansible_host=xxxx ansible_user=xxxx ansible_ssh_private_key_file=~/.ssh/xxxx
